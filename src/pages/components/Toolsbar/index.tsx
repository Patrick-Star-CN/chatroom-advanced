import { Button } from 'antd';

export default function Toolsbar() {
  return (
    <div className="tools-bar">
      <Button>表情</Button>
      <Button>图片</Button>
      <Button>文件</Button>
    </div>
  );
}
